menu: "Diagnostic → Powertrain → Engine (ME9.6) → Read Codes"
target_ecu: $07E0/$07E8 (OBD-II) with DTC SF replies on $05E8
captured_from:
  shim_log: cstech2win_shim_20260513-115320.log
  range_ms: [92400, 92800]
  vehicle_profile: SAAB 9-3 (field car, 2009-2011 ME9.6, VIN prefix YS3FH46U…)
status: captured (22 DTCs returned in one frame burst)
services:
  - 0xA9  # ReadDiagnosticInformation (GMW3110 §9)
    # subfunction:
    #   0x81  ReadStatusOfDTCByStatusMask
    # mask observed:
    #   0x0C  (status & (pending | test_passed_since_clear)) — wider than
    #         the body-bus "Check Codes" 0x10/0x12; pulls back history.

steps:
  - step: request_dtcs
    tx: [0xA9, 0x81, 0x0C]
    can_id: $07E0
    rx:
      can_id: $05E8                # OBD-II single-frame DTC reply address
      frame_format:
        byte_0: 0x81               # subfunction echo
        bytes_1_3: dtc_code
        byte_4: status_mask
        bytes_5_7: 00 00 00
      delivery: "ME9.6 streams one SF per DTC at ~10ms cadence. No
                 responsePending observed on $07E8 — engine ECM replies
                 immediately."
      terminator: "81 00 00 00 FF 00 00 00"
    timeout_ms: 1500

# All 22 frames captured from this single request. Status byte interpretation
# follows GMW3110 §9.3 — see commands/saab/check_codes_read_dtcs.yaml#status_bits.
observed_dtcs:
  # Descriptions sourced from z90.pl/saab/dtc/ — community SAAB DTC reference
  # (model 9-3 9440, MY 2009). Cross-referenced 2026-05-13.
  - { raw: "C0 01 00", code: "U0001-00", status: 0x19, desc: "TCM was Missing on Bus" }
  - { raw: "C1 22 00", code: "U0122-00", status: 0x19, desc: "ESP Missing on BUS" }
  - { raw: "C2 12 00", code: "U0212-00", status: 0x19, desc: "CIM Missing on BUS" }
  - { raw: "C1 55 00", code: "U0155-00", status: 0x25, desc: "MIU Missing on BUS (occurrence 1)" }
  - { raw: "C1 55 00", code: "U0155-00", status: 0x25, desc: "MIU Missing on BUS (occurrence 2)" }
  - { raw: "06 26 00", code: "P0626-00", status: 0x25, desc: "Generator F terminal Circuit, Short to B+" }
  - { raw: "06 25 00", code: "P0625-00", status: 0x25, desc: "Generator F terminal Circuit, Open/Short to Ground" }
  - { raw: "04 20 00", code: "P0420-00", status: 0x25, desc: "Three Way Catalytic Converter. Malfunction." }
  - { raw: "02 34 00", code: "P0234-00", status: 0x25, desc: "Turbocharger Overboost Detected" }
  - { raw: "02 99 00", code: "P0299-00", status: 0x25, desc: "Turbocharger Underboost Detected" }
  - { raw: "06 96 00", code: "P0696-00", status: 0x25, desc: "Cooling Fan 3 Control Circuit, Short to B+" }
  - { raw: "06 95 00", code: "P0695-00", status: 0x25, desc: "Cooling Fan 3 Control Circuit, Open/Short to Ground" }
  - { raw: "04 82 00", code: "P0482-00", status: 0x25, desc: "Cooling Fan 3 Control Circuit, Open" }
  - { raw: "20 97 00", code: "P2097-00", status: 0x25, desc: "Post Catalyst Fuel Trim System, High Limit Bank 1" }
  - { raw: "20 96 00", code: "P2096-00", status: 0x25, desc: "Post Catalyst Fuel Trim System, Low Limit Bank 1" }
  - { raw: "21 96 00", code: "P2196-00", status: 0x25, desc: "Short Term Fuel Trim. Min Value, Air/Fuel too Rich." }
  - { raw: "21 95 00", code: "P2195-00", status: 0x25, desc: "Short Term Fuel Trim. Max Value, Air/Fuel too Lean." }
  - { raw: "04 11 00", code: "P0411-00", status: 0xBF, desc: "Secondary Air Injection System. Incorrect Airflow. — HEAVIEST status (all bits + warning indicator)" }
  - { raw: "C1 22 00", code: "U0122-00", status: 0x3F, desc: "ESP Missing on BUS (re-appears with fuller status — distinct entry)" }
  - { raw: "05 00 00", code: "P0500-00", status: 0x09, desc: "Vehicle Speed. Incorrect Signal. — only active + cal-supported, no MIL yet" }
terminator: "81 00 00 00 FF 00 00 00"

clinical_picture: |
  Reading these together: the engine has been losing comm with TCM, ESP,
  CIM, and MIU — bus dropouts. On top of that, multiple paired pin-fault
  codes (P0625/P0626 alternator, P0695/P0696 cooling fan 3 circuit) suggest
  wiring trouble in the engine-bay harness. P0234/P0299 paired indicates
  the turbo control loop is unstable. P0411 (secondary air) is the only
  DTC currently requesting MIL.

dtc_byte_layout: |
  Same algorithm as check_codes_read_dtcs.yaml — transcribed from
  Trionic8.cs:GetDtcDescription:

    byte 1 bits 7-6  →  letter      (0=P, 1=C, 2=B, 3=U)
    byte 1 bits 5-4  →  digit_2     (decimal 0-3)
    byte 1 bits 3-0  →  digit_3     (hex 0-F)
    byte 2 high nib  →  digit_4     (hex 0-F)
    byte 2 low nib   →  digit_5     (hex 0-F)
    byte 3           →  failureType (printed as 2-digit hex suffix)

  Worked examples (this capture):
    `04 20 00` → (0,0,4,2,0)+0x00 → P0420-00  (catalyst efficiency)
    `C1 22 00` → (3,0,1,2,2)+0x00 → U0122-00  (NOT C1122 — high 2 bits of
                                              byte 1 = 0xC0 >> 6 = 3 = 'U')
    `21 96 00` → (0,2,1,9,6)+0x00 → P2196-00  (O2 sensor stuck)

  Decoder script: tools/decode_gmw3110_dtc.py.

addressing_split: |
  ME9.6 engine ECU uses OBD-II standardised addressing, distinct from the
  SAAB manufacturer-alias range ($024X / $054X / $064X) used by body modules
  and the T8 engine in check_codes_read_dtcs.yaml:

    Request:        $07E0
    SF DTC reply:   $05E8     ← used here
    Diag reply:     $07E8     ← used for NRC / MF reads

  This is the same split-reply pattern as the SAAB aliases but on the
  OBD-II side. Per Trionic's `FilterIdECU = { 0x7E0, 0x7E8, 0x5E8 }`.

contrast_with_check_codes_yaml: |
  - check_codes_read_dtcs.yaml is the bus-wide "Check Codes" menu — sweeps
    every ECU with masks 0x10/0x12.
  - This entry is a SINGLE-ECU read with mask 0x0C, surfacing pending and
    just-cleared DTCs that the bus sweep wouldn't show.
  - No `20` (ReturnToNormalOperation) preamble in this single-ECU request —
    Tech2Win assumes the session is already in default state for this
    menu, unlike the bus-wide Check Codes flow which resets each ECU.

notes: |
  - This is a *dirty* engine — 21 unique DTCs returned. Most have status
    0x25 = (testNotPassedSinceCurrentPowerUp | testNotPassedSinceDTCCleared
    | DTCSupportedByCalibration). Codes are stored and were re-tested
    (and failed) on the current power cycle.
  - Two frames carry `C1 55 00` U0155-00 — most likely two distinct
    historical entries the ECM exposes separately, not a shim duplicate.
  - `P0411` (secondary air system) carries status 0xBF — every bit set
    including `warningIndicatorRequestedState`. That's the only DTC
    actively requesting MIL in this snapshot.
  - DTC names decoded via Trionic's algorithm; failure-mode (sub-byte 3)
    interpretation per SAE J2012 — best-effort labels in `note:` field.
  - U-codes (U0001, U0122, U0155, U0212) all indicate lost-comm with
    other modules — consistent with the ME9.6 having seen a bus dropout
    or modules being offline at some point.

open_questions:
  - Why mask 0x0C here specifically? Tech2Win could read 0x10 (confirmed)
    instead. Possibly the ME9.6 menu surfaces pending / transient DTCs
    where the body-module sweep doesn't.
  - The duplicate `C1 22 00` (once at status 0x19, once at 0x3F) — is
    that one DTC re-reported, or two separate stored failures sharing
    the same code?
  - Whether OBD-II $07E0 supports the same mask family (0x10/0x12) the
    SAAB body bus does, or whether ME9.6 uses a different mask set.
